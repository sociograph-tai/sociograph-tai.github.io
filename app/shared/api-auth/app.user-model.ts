export interface User {
    id: Number,
    name: String,
    firstName: String,
    lastName: String,
    email: String,
    password: String,
    enabled: Boolean,
    createdAt: Date,
    updatedAt: Date
}